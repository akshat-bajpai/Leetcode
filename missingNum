class Solution {
  public:
    int missingNum(vector<int>& arr) {
        // code here
        int x=0;
        for (int i=0;i<arr.size();i++){
            x=x^arr[i];
        }
        for (int i=1;i<=arr.size()+1;i++){
            x=x^i;
        }
        return x;
    }
};
