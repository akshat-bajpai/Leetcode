class Solution {
public:
    int minBitFlips(int start, int goal) {
        int bits = (start ^ goal);
        int count = 0;
        while(bits)
        {
            bits = bits & (bits-1);
            count++;
        }
        return count;
    }

};
